rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permite lectura y escritura total a cualquier usuario autenticado en toda la base de datos
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
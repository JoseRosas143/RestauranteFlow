
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Permitir acceso total para el MVP. En producción, esto debe restringirse.
      allow read, write: if true;
    }
  }
}

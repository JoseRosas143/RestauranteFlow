rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Regla global ultra-permisiva para desarrollo: permite todo a cualquier usuario autenticado
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}